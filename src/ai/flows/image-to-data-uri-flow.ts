'use server';
/**
 * @fileOverview This flow is deprecated and no longer used.
 * It was intended to convert image URLs to Data URIs to solve CORS issues,
 * but a client-side solution is now in place.
 */
