{
  "entities": {
    "Vehicle": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Vehicle",
      "type": "object",
      "description": "Represents a vehicle available for sale in the LuxDrive Catalog, including its technical specifications, marketing details, and sales status.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Vehicle entity."
        },
        "brand": {
          "type": "string",
          "description": "The brand or manufacturer of the vehicle (e.g., 'BMW', 'Mercedes-Benz')."
        },
        "model": {
          "type": "string",
          "description": "The specific model of the vehicle (e.g., 'Serie 3', 'Classe C')."
        },
        "version": {
          "type": "string",
          "description": "The specific version or trim level of the vehicle (e.g., '320d Business', 'C 220 d Premium')."
        },
        "data_immatricolazione": {
          "type": "string",
          "format": "date",
          "description": "The first registration date of the vehicle."
        },
        "mileage": {
          "type": "number",
          "description": "The total distance the vehicle has traveled, in kilometers."
        },
        "fuelType": {
          "type": "string",
          "description": "The type of fuel the vehicle uses (e.g., 'Benzina', 'Diesel', 'Elettrica')."
        },
        "transmissionType": {
          "type": "string",
          "description": "The type of transmission in the vehicle (e.g., 'Manuale', 'Automatica')."
        },
        "horsepower": {
          "type": "number",
          "description": "The engine's power output in horsepower (CV)."
        },
        "exteriorColor": {
          "type": "string",
          "description": "The exterior color of the vehicle."
        },
        "price": {
          "type": "number",
          "description": "The selling price of the vehicle."
        },
        "description": {
          "type": "string",
          "description": "A comprehensive commercial description of the vehicle, highlighting its features and selling points. This can be AI-assisted."
        },
        "coverImageUrl": {
          "type": "string",
          "description": "The URL of the primary image used as a cover for the vehicle listing card.",
          "format": "uri"
        },
        "imageUrls": {
          "type": "array",
          "description": "An array of URLs pointing to additional images of the vehicle for the full gallery.",
          "items": {
            "type": "string"
          }
        },
        "canvaLink": {
          "type": "string",
          "description": "A URL to an external Canva presentation or extended photo gallery for the vehicle.",
          "format": "uri"
        },
        "status": {
          "type": "string",
          "description": "The current sales status of the vehicle (e.g., 'Pubblicato', 'Opzionato', 'Venduto').",
          "enum": ["In vendita", "Venduto", "Prenotato"]
        },
        "consultantName": {
          "type": "string",
          "description": "The full name of the sales consultant assigned to this vehicle."
        },
        "consultantPhoneNumber": {
          "type": "string",
          "description": "The phone number of the sales consultant, primarily used for direct WhatsApp contact."
        },
        "slug": {
          "type": "string",
          "description": "A URL-friendly, unique identifier (slug) for the vehicle, generated for SEO and dynamic routing (e.g., 'marca-modello-anno-id')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when this vehicle record was first created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when this vehicle record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "brand",
        "model",
        "data_immatricolazione",
        "mileage",
        "fuelType",
        "transmissionType",
        "horsepower",
        "exteriorColor",
        "price",
        "description",
        "coverImageUrl",
        "imageUrls",
        "status",
        "consultantName",
        "consultantPhoneNumber",
        "slug",
        "createdAt",
        "updatedAt"
      ]
    },
    "AdminRole": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AdminRole",
      "type": "object",
      "description": "Represents an admin role. The existence of a document for a user in the Admin collection grants them full administrative privileges.",
      "properties": {
        "assignedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the admin role was assigned."
        }
      }
    },
    "SellerRole": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SellerRole",
      "type": "object",
      "description": "Represents a seller role. The existence of a document for a user in the sellertype collection grants them seller privileges. Contains the seller's specific branding type.",
      "properties": {
        "assignedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the seller role was assigned."
        },
        "sellerType": {
          "type": "string",
          "description": "The branding profile type for this seller.",
          "enum": [
            "TIPO_A",
            "TIPO_B",
            "TIPO_C",
            "TIPO_D"
          ]
        }
      }
    },
    "Form": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Form",
      "type": "object",
      "description": "Represents a document or form, such as a sales agreement or privacy policy.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the form document."
        },
        "title": {
          "type": "string",
          "description": "The display name of the form (e.g., 'Contratto di Vendita')."
        },
        "category": {
          "type": "string",
          "description": "The category of the form, used for organization.",
          "enum": ["cliente", "commerciante"]
        },
        "fileUrl": {
          "type": "string",
          "format": "uri",
          "description": "The public URL to the stored file in Firebase Storage."
        },
        "fileName": {
            "type": "string",
            "description": "The name of the uploaded file."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the form was uploaded."
        }
      },
      "required": ["id", "title", "category", "fileUrl", "fileName", "createdAt"]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a registered user profile in the system, used for management purposes.",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the user account was created."
        }
      },
      "required": ["email", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/vehicles/{vehicleId}",
        "definition": {
          "entityName": "Vehicle",
          "schema": {
            "$ref": "#/backend/entities/Vehicle"
          },
          "description": "Primary collection for all vehicle listings in the LuxDrive Catalog. Documents are publicly readable to display the car catalog and individual car details. Write operations (create, update, delete) are restricted to authenticated administrators. Includes denormalized 'slug' for SEO and 'status' for UI state control.",
          "params": [
            {
              "name": "vehicleId",
              "description": "Unique identifier for a specific vehicle document."
            }
          ]
        }
      },
      {
        "path": "/Admin/{userId}",
        "definition": {
          "entityName": "AdminRole",
          "schema": {
            "$ref": "#/backend/entities/AdminRole"
          },
          "description": "Dedicated collection for administrative roles. The existence of a document at this path signifies that the user (identified by '{userId}') has administrative privileges to create, update, and delete vehicle listings. Documents are typically empty or contain minimal metadata.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who is assigned the administrative role."
            }
          ]
        }
      },
      {
        "path": "/sellertype/{userId}",
        "definition": {
          "entityName": "SellerRole",
          "schema": {
            "$ref": "#/backend/entities/SellerRole"
          },
          "description": "Collection for seller roles. The existence of a document grants seller privileges: read all vehicle data, and update specific fields (status, description). Each document should contain a 'sellerType' to determine branding.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who is assigned the seller role."
            }
          ]
        }
      },
      {
        "path": "/forms/{formId}",
        "definition": {
          "entityName": "Form",
          "schema": {
            "$ref": "#/backend/entities/Form"
          },
          "description": "Collection to store metadata for uploaded forms and documents. Read is allowed for any authenticated user, while create and delete operations are restricted to administrators.",
          "params": [
            {
              "name": "formId",
              "description": "Unique identifier for a specific form document."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Collection storing basic profile information for all registered users. This allows administrators to view and manage users.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, matching their Firebase Auth UID."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure for LuxDrive Catalog prioritizes secure, scalable, and debuggable authorization by adhering to the core design principles. It leverages **Structural Segregation** and **DBAC (Database-Backed Access Control)** to clearly separate public data from administrative functions.\n\n**Authorization Independence (CRITICAL):**\nVehicle documents are stored in the top-level `/vehicles/{vehicleId}` collection. Their authorization is self-contained: they are publicly readable, and write operations (create, update, delete) are restricted to administrators. There are no subcollections whose access depends on parent document attributes, thus eliminating the need for `get()` calls in security rules for hierarchical authorization. Admin roles are managed in a separate, independent collection (`/Admin/{userId}`), ensuring that checking for administrative privileges is a single, atomic lookup and does not create hierarchical authorization dependencies within the vehicle data itself. This allows for atomic operations (transactions/batches) on vehicle documents without breaking security rule evaluations.\n\n**QAPs (Rules are not Filters):**\nFor the `/vehicles/{vehicleId}` collection, the `read` permission is `allow read: if true;`. This means that `list` queries on the `/vehicles` collection will return all documents that match the query filters (e.g., `where('brand', '==', 'BMW')`) without needing any additional filtering logic in the security rules. This design enables efficient client-side filtering and sorting as described in the requirements, fully supporting the Query-as-Permission principle. Security rules serve solely as authorization gates, not data filters, which is crucial for scalable performance and security.\n\n**Structure Breakdown:**\n1.  **`/vehicles/{vehicleId}`:** This primary collection stores all `Vehicle` entities, including all technical specifications, marketing details, and sales status information. Each document in this collection represents a single car available for sale.\n    *   **Security Posture:** Publicly readable (all authenticated and unauthenticated users can view) for the 'Auto in Vendita' catalog. Write access (create, update, delete) is restricted to authenticated users who possess an administrative role.\n    *   **Fields:** Contains all fields from the `Vehicle` schema, including `id`, `brand`, `model`, `year`, `mileage`, `fuelType`, `transmissionType`, `horsepower`, `exteriorColor`, `price`, `description`, `coverImageUrl`, `imageUrls`, `canvaLink`, `status`, `consultantName`, `consultantPhoneNumber`, `slug`, `createdAt`, and `updatedAt`.\n    *   **Denormalization:** The `slug` field is denormalized within each `Vehicle` document to facilitate SEO-friendly dynamic routing without requiring additional lookups. The `status` field explicitly models the vehicle's sales state, which influences UI behavior (e.g., disabling the contact button).\n\n2.  **`/Admin/{userId}`:** This collection is dedicated to managing administrative roles within the system. While not explicitly provided in the initial `backend.entities`, an `AdminRole` entity is conceptually introduced here to fulfill the DBAC mandate and enable secure administrative actions.\n    *   **Security Posture:** Read/write access is highly restricted, typically to a super-admin or internal processes. The mere existence of a document at `/Admin/{request.auth.uid}` grants administrative privileges for managing vehicles.\n    *   **Fields:** Documents in this collection can be empty or contain minimal metadata, as their existence itself signifies the role. For the purpose of this design, it maps to a conceptual `AdminRole` entity whose presence grants privileges."
  }
}
